{
  "name": "vue-task",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "npx json-server --watch db.json --port 8080 --delay 1000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
